# Admin Panel Tests

This directory contains test scripts for the Admin Panel (Next.js) application.

## 📋 Test Status

Currently, no admin panel tests are available. Tests will be added here as the admin panel features are developed and tested.

## 🚀 Running Tests

Once tests are added, run them from the `system-pos` root directory:

```bash
# Run all admin tests
npx tsx tests/scripts/admin/run-all.ts

# Run a specific test
npx tsx tests/scripts/admin/test-specific-feature.ts
```

## 📝 Planned Tests

- Admin authentication tests
- Dashboard functionality tests
- Employee management tests
- Product management tests
- Report generation tests
- Settings management tests

## ⚙️ Prerequisites

- Admin panel running (Next.js dev server)
- API server running
- Database accessible

